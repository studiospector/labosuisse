{% extends '@PathViews/templates/_default.twig' %}

{% set bodyClass = 'lb-page' %}

{% block main %}

    {% if page_title %}
        <h1 style="position: absolute; font-size: 2px; margin: 0px; text-indent: -9999px;">{{ page_title }}</h1>
    {% endif %}

    {{ content|raw }}

{% endblock %}
