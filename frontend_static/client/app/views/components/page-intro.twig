<div class="container text-center">
    <div class="infobox">
        {% if title %}
            <h1 class="infobox__subtitle h2">{{ title }}</h1>
        {% endif %}
        {% if description %}
            <p class="infobox__paragraph">{{ description }}</p>
        {% endif %}
    </div>
</div>
