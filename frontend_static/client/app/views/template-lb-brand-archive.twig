{% extends '@PathViews/templates/_default.twig' %}

{% set bodyClass = 'taxonomy-lb-brand-archive' %}

{% block main %}

    {# Content #}
    {{ content|raw }}

    {# Filters #}
    {% if filters|length > 0 %}
        {% include '@PathViews/components/filters.twig' with filters only %}
        {% include '@PathViews/components/separator.twig' with {variants: ['medium']} only %}
    {% endif %}

    {# Posts Grid #}
    <div class="taxonomy-lb-brand-archive__grid container">
        {% include '@PathViews/components/cards-grid-default.twig' with {
            col: 4,
            items: items,
        } only %}
    </div>

{% endblock %}
