import Component from '@okiba/component'
import { qs, on, off } from '@okiba/dom'

const ui = {}

class AnimationParallax extends Component {
    constructor({ options, ...props }) {
        super({ ...props, ui })

        
    }
}

export default AnimationParallax
